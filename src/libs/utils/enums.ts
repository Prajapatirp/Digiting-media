export enum Role {
  Admin = 'Admin',
  Dealer = 'Dealer',
}

export enum ResponseData {
  SUCCESS = 'Success',
  ERROR = 'Error',
}

export enum ModuleName {
  user = 'User',
  cityList = 'cityList',
  stateList = 'stateList',
  package = 'Package',
  ContactUs = 'ContactUs',
  Deal = 'Deal'
}

export enum DealStatus {
  open = 'Open',
  close = 'Close',
}
